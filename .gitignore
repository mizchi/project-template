# Nix
result
result-*
.direnv/

# APM installed modules / deployed skills.
# apm.yml + apm.lock.yaml are tracked; re-deploy with `apm install`.
apm_modules/
.claude/skills/
.github/skills/

# pkspec: `schemas/` is regenerated by `pkspec init` (the `spec` task);
# `.pkspec/` holds per-test timing history. Test.pkl is tracked.
schemas/
.pkspec/

# Local env overrides
.env.local

# Claude Code per-user local settings (machine-specific permissions etc.)
.claude/settings.local.json
