use flake
# Idempotent git-hook install (no-op when already present).
pkf hooks install >/dev/null 2>&1 || true
# Load local overrides (API keys etc.) when present. Never commit .env.local.
dotenv_if_exists .env.local
